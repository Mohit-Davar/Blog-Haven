<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Haven</title>
    <link rel="shortcut icon" href="/Images/LogoOneWithBg.jpg" type="image/x-icon">
    <link rel="stylesheet" href="/CSS/output.css">

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">
</head>

<body class="bg-theme-two p-5">
    <div class="max-w-lg mx-auto mt-5 bg-white rounded-2xl shadow-md p-5 font-Roboto">
        <img class="w-32 h-32 rounded-full mx-auto object-center object-cover"
            src="<%= locals.ProfileUser.profileImg %>" alt="Profile picture">
        <h2 class="text-center text-2xl font-semibold mt-3 text-theme-one">
            <%= locals.ProfileUser.name.firstName %>
                <%= locals.ProfileUser.name.lastName %>
        </h2>
        <p class="text-center text-gray-600 mt-1">
            <%=locals.ProfileUser.role%>
        </p>
        <div class="mt-5">
            <h3 class="text-xl font-semibold text-theme-one">Bio</h3>
            <p class="text-gray-600 mt-2 break-words">
                <%= locals.ProfileUser.bio %>
            </p>
        </div>
    </div>
    <div class="flex justify-center mt-5">
        <a href="/blogs" class="bg-theme-one  text-white px-4 py-2 rounded-2xl font-Roboto">Back</a>
    </div>
    <h1 class="mx-auto w-fit my-10 text-white text-5xl break-words text-center font-Lobster">My Blogs</h1>
    <% if (locals.blogs) { %>
        <div class="all-blogs flex items-stretch justify-center gap-5 flex-wrap">
            <% locals.blogs.forEach(blog=> { %>
                <div class="bg-white rounded-lg shadow-lg overflow-hidden max-w-lg w-full">
                    <a href="/blogs/blog/<%=blog._id%>">
                        <img src="<%= blog.thumbnailImage %>" alt="Thumbnail Image" class="w-full h-64 object-cover">
                    </a>
                    <div class="p-6">
                        <h2 class="text-2xl font-bold text-gray-800 mb-2">
                            <%= blog.title %>
                        </h2>
                        <p class="text-gray-700 leading-tight mb-4">
                            <%= blog.previewText %>
                        </p>
                        <div class="flex justify-between items-center">
                            <div class="flex items-center">
                                <img src="<%= ProfileUser.profileImg %>" alt="Avatar"
                                    class="w-8 h-8 rounded-full mr-2 object-cover">
                                <span class="text-gray-800 font-semibold">
                                    <%= ProfileUser.name.firstName %> <%= ProfileUser.name.lastName %>
                                </span>
                            </div>
                            <span class="text-gray-600">
                                <%= blog.createdAt.toLocaleDateString('en-US', { month: 'long' , day: 'numeric' })%>
                            </span>
                        </div>
                    </div>
                </div>
                <% }) %>
        </div>
        <% } %>
</body>

</html>